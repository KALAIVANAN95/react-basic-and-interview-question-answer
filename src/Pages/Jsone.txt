

### 1. What is JavaScript?

i) High level, interpreted language.
ii) Creating dynamic interaction with web pages.

Example:
document.getElementById("demo").innerHTML = "Hello World";

---

### 2. What is Data Types in JavaScript?

i) Primitive Types
string, number, boolean, null, undefined (also symbol, bigint)

ii) Reference Types
object, functions, arrays

Example:
let name = "Kala";      // String
let age = 25;           // Number
let isActive = true;    // Boolean
let value = null;       // Null
let data;               // Undefined

const user = { name: "Kala" };
const arr = [1, 2, 3];

---

### 3. Difference between null and undefined?

i) Null - Intentional null (empty) value of variable.
ii) Undefined - Declared variable without any value.

Example:
let a;
console.log(a); // undefined

let b = null;
console.log(b); // null

---

### 4. How to declare variable in JS?

Can declare variable using

i) var = function scoped
ii) let = block scoped
iii) const = block scoped - cannot be reassigned

Example:
var a = 10;
let b = 20;
const c = 30;

console.log(a, b, c);

---

### 5. What is Closure in JS?

A closure allows inner functions to access outer function variables even after execution.

Example:
function outer() {
let count = 0;

return function inner() {
count++;
console.log(count);
};
}

const counter = outer();

counter(); // 1
counter(); // 2

---

### 6. Difference between == and ===?

i) == (loose equality): compares values after type coercion.
ii) === (strict equality): compares both value and type.

== checks value only.
=== checks both value and datatype.

I prefer === to avoid unexpected type coercion.

Example:
console.log(5 == "5");   // true
console.log(5 === "5");  // false

---

### 7. What is Hoisting?

i) Variable and function declarations moved to the top during execution.

Example:
console.log(a); // undefined

var a = 10;

sayHello();

function sayHello() {
console.log("Hello");
}

---

### 8. Ways to loop through an array?

using various methods such as

i) for
ii) forEach
iii) for...of

Example:
const arr = [10, 20, 30];

for (let i = 0; i < arr.length; i++) {
console.log(arr[i]);
}

arr.forEach(item => console.log(item));

for (const item of arr) {
console.log(item);
}

---

### 9. What is Promise in JS?

Promises are used to handle asynchronous operations with success and failure states.

It has methods like .then() and .catch().

It will be in one of the 3 possible states

i) Fulfilled
ii) Rejected
iii) Pending

Example:
const promise = new Promise((resolve, reject) => {
resolve("Success");
});

promise
.then(result => console.log(result))
.catch(error => console.log(error));

---

### 10. Ways to define function?

i) Function declaration
ii) Function expression
iii) Self invoking function
iv) Object method
v) Arrow function

Example:
function add(a, b) {
return a + b;
}

const sub = function(a, b) {
return a - b;
};

const mul = (a, b) => a * b;

console.log(add(2, 3));
console.log(sub(5, 2));
console.log(mul(2, 4));

---
Sure! Here's **Part 2 (Questions 11–20)** in the same copy-paste format.

---

### 11. Currying?

Currying is a technique of evaluation function with multiple arguments into sequence of function with single argument.

Example:

```javascript
function add(a) {
  return function(b) {
    return a + b;
  };
}

console.log(add(2)(3)); // 5
```

---

### 12. What is this keyword?

This keyword is a reference variable that refers to the current object.

Example:

```javascript
const user = {
  name: "Kala",
  showName() {
    console.log(this.name);
  }
};

user.showName(); // Kala
```

---

### 13. Diff btwn forEach and map method?

The map() method returns a new array, whereas the forEach() method does not return a new array.

or

map() is used to iterate arrays and return a new transformed array.

Example:

```javascript
const numbers = [1, 2, 3];

// map
const doubled = numbers.map(num => num * 2);
console.log(doubled); // [2, 4, 6]

// forEach
numbers.forEach(num => console.log(num));
```

---

### 14. apply bind call

call() – invokes the function immediately, arguments passed one by one.

apply() – invokes the function immediately, arguments passed as an array.

bind() – does not invoke immediately, returns a new function with this fixed, and you can pass arguments.

Example:

```javascript
const person = {
  name: "Kala"
};

function greet(city) {
  console.log(this.name + " " + city);
}

greet.call(person, "Chennai");
greet.apply(person, ["Chennai"]);

const fn = greet.bind(person, "Chennai");
fn();
```

---

### 15. async and await

"async and await make promises easier to write."

async makes a function return a Promise.

await pauses execution until the Promise resolves (or rejects).

Example:

```javascript
async function getData() {
  const data = await Promise.resolve("Hello");
  console.log(data);
}

getData();
```

---

### 16. "use strict"

"use strict" enables strict mode in JavaScript, which helps catch errors and enforces safer coding.

Example:

```javascript
"use strict";

let a = 10;
console.log(a);
```

---

### 17. Map

map() is used to iterate arrays and return a new transformed array.

Example:

```javascript
const numbers = [1, 2, 3];

const result = numbers.map(num => num * 2);

console.log(result); // [2, 4, 6]
```

---

### 18. Filter

filter() returns array elements that satisfy a condition.

Example:

```javascript
const numbers = [1, 2, 3, 4, 5];

const even = numbers.filter(num => num % 2 === 0);

console.log(even); // [2, 4]
```

---

### 19. Reduce?

reduce() is used to reduce an array to a single value (like sum, total, or object) by applying a function.

Example:

```javascript
const numbers = [1, 2, 3, 4];

const sum = numbers.reduce((total, num) => total + num, 0);

console.log(sum); // 10
```

---

### 20. setTimeout vs setInterval

Both are used to run code after a delay, but behavior is different.

**setTimeout**
Runs once after a given delay.

Example:

```javascript
setTimeout(() => {
  console.log("Hello");
}, 2000);
```

**setInterval**
Runs repeatedly at every interval.

Example:

```javascript
setInterval(() => {
  console.log("Hello");
}, 2000);
```

---

Sure! Here's **Part 3 (Questions 21–34)** in the same copy-paste format.

---

### 21. Functions

Functions are reusable blocks of code used to perform specific tasks.

Example:

```javascript
function greet() {
  console.log("Hello");
}

greet(); // Hello
```

---

### 22. Conditions

Conditional statements are used for decision making in JavaScript.

Example:

```javascript
const age = 20;

if (age >= 18) {
  console.log("Eligible to vote");
} else {
  console.log("Not eligible");
}
```

---

### 23. Loops

Loops are used to execute code repeatedly based on conditions.

Example:

```javascript
for (let i = 1; i <= 5; i++) {
  console.log(i);
}
```

---

### 24. DOM Manipulation

DOM Manipulation allows JavaScript to dynamically update HTML, CSS, and page content.

Example:

```javascript
document.getElementById("title").innerHTML = "Welcome";
```

---

### 25. Callback Function

A callback function is passed as an argument to another function and executed later.

2 types callback iruku:

1. Synchronous Callback
   Immediate execute.

Example:

```javascript
const arr = [1, 2, 3];

arr.forEach(item => console.log(item));
```

2. Asynchronous Callback

Example:

```javascript
setTimeout(() => {
  console.log("Done");
}, 2000);
```

---

### 26. Event Loop

JavaScript uses the event loop to handle asynchronous operations.

Example:

```javascript
console.log("Start");

setTimeout(() => {
  console.log("Inside Timeout");
}, 1000);

console.log("End");

// Output:
// Start
// End
// Inside Timeout
```

---

### 27. Scope

Scope determines where variables can be accessed in JavaScript.

Types of Scope

Global Scope → Accessible anywhere.

Function Scope → Accessible only inside the function.

Block Scope → Accessible only inside {} (with let and const).

Example:

```javascript
let a = 10;

function demo() {
  let b = 20;
  console.log(a);
  console.log(b);
}

demo();
```

---

### 28. Prototype

Prototype enables inheritance in JavaScript objects.

Example:

```javascript
const arr = [1, 2, 3];

console.log(arr.length);

// length comes from Array prototype
```

---

### 29. Debounce

Debouncing delays function execution until the user stops triggering the event.

Example:

```javascript
Search Box

User typing...
After user stops typing for 500ms,
Search API will be called once.
```

---

### 30. Throttle

Throttling means a function can run only once in a specific time period, even if it is triggered many times.

Example:

```javascript
Scroll Event

User continuously scrolls,
Function executes only once every 1 second.
```

---

### 31. Local Storage

localStorage stores data permanently in the browser.

Example:

```javascript
localStorage.setItem("name", "Kala");

console.log(localStorage.getItem("name"));
```

---

### 32. Session Storage

sessionStorage stores data only for the browser session.

Example:

```javascript
sessionStorage.setItem("user", "Kala");

console.log(sessionStorage.getItem("user"));
```

---

### 33. Cookies

Cookies store small pieces of browser data mainly for authentication and sessions.

Example:

```javascript
document.cookie = "username=Kala";
```

---

### 34. call/apply/bind

call, apply and bind are used to control the `this` context of functions.

Example:

```javascript
const person = {
  name: "Kala"
};

function greet(city) {
  console.log(this.name + " " + city);
}

greet.call(person, "Chennai");

greet.apply(person, ["Chennai"]);

const fn = greet.bind(person, "Chennai");
fn();
```

---

Sure! Here's **Part 4** in the same copy-paste format.

---

### 35. ES Modules

ES Modules allow splitting and reusing JavaScript code using import and export.

Example:

```javascript
// math.js
export const add = (a, b) => a + b;

// app.js
import { add } from "./math.js";

console.log(add(2, 3)); // 5
```

---

### 36. Shallow vs Deep Copy

Shallow copy shares references, whereas deep copy creates independent copies.

Example:

```javascript
const obj = {
  name: "Kala",
  address: {
    city: "Chennai"
  }
};

// Shallow Copy
const copy1 = { ...obj };

// Deep Copy
const copy2 = structuredClone(obj);
```

---

### 37. Why use objects?

Objects are used to store related data in key-value pairs.

Example:

```javascript
const user = {
  name: "Kala",
  age: 25
};

console.log(user.name); // Kala
```

---

### 38. Array of Objects?

Multiple objects inside array.

Example:

```javascript
const users = [
  { id: 1, name: "Kala" },
  { id: 2, name: "Raj" }
];

console.log(users[0].name); // Kala
```

---

### 39. Destructuring?

Destructuring is used to extract values from arrays or objects easily.

Example:

```javascript
const user = {
  name: "Kala",
  age: 25
};

const { name, age } = user;

console.log(name); // Kala
console.log(age);  // 25
```

Array Example:

```javascript
const numbers = [10, 20];

const [a, b] = numbers;

console.log(a); // 10
console.log(b); // 20
```

---

### 40. What is spread operator?

Spread operator is used to copy, merge, or expand arrays and objects.

Example:

```javascript
const arr1 = [1, 2, 3];

const arr2 = [...arr1, 4, 5];

console.log(arr2);
// [1,2,3,4,5]
```

Object Example:

```javascript
const user = {
  name: "Kala"
};

const details = {
  ...user,
  age: 25
};

console.log(details);
```

---

### 41. Ternary - Short if else

Ternary operator is a short form of if else condition.

Example:

```javascript
const age = 20;

const result = age >= 18 ? "Eligible" : "Not Eligible";

console.log(result);
```

---

### 42. Rest operator

Rest operator is used to collect multiple values into a single array.

Example:

```javascript
const values = [1, 2, 3, 4, 5];
const [a, b, ...rest] = values;
console.log(a);    // 1
console.log(b);    // 2
console.log(rest); // [3,4,5]
```

Function Example:

```javascript
function add(...numbers) {
  console.log(numbers);
}

add(10, 20, 30);
// [10,20,30]
```

---

### 43. Event Delegation

Event Delegation is attaching a single event listener to a parent element to handle events from multiple child elements using event bubbling.

Example:

```javascript
<ul id="list">
  <li>Apple</li>
  <li>Mango</li>
</ul>

document.getElementById("list").addEventListener("click", function(event) {
  console.log(event.target.innerText);
});
```

---

### 44. Reverse Array

Example:

```javascript
const arr = [1, 2, 5];

const res = arr.reverse();

console.log(res);
// [5,2,1]
```

---

### 45. Reverse String

Example:

```javascript
function reverseStr(str) {
  return str.split("").reverse().join("");
}

console.log(reverseStr("Hello"));
// olleH
```

---

### 46. Palindrome

Example:

```javascript
function palindrome(str) {
  return str === str.split("").reverse().join("");
}

console.log(palindrome("madam"));
// true
```

---

### 47. Remove Duplicates

Example:

```javascript
const array = [100, 1, 2, 3, 2, 3, 4, 4, 5];

const unique = [...new Set(array)];

console.log(unique);
// [100,1,2,3,4,5]
```

---

### 48. Find Maximum Number

Example:

```javascript
const array = [100, 1, 2, 3, 2, 3, 4, 4, 5];

const max = Math.max(...array);

console.log(max);
// 100
```
---

### 49. var vs let vs const?

var is function scoped.

let and const are block scoped.

const cannot be reassigned.

I mostly use const by default and let when values need updates.

Example:

```javascript
var a = 10;
let b = 20;
const c = 30;

b = 40; // ✅ Allowed
// c = 50; ❌ Error

console.log(a);
console.log(b);
console.log(c);
```

---

### 50. map vs filter?

map transforms data.

or

map() is used to iterate arrays and return a new transformed array.

filter returns matching data based on condition.

or

filter() returns array elements that satisfy a condition.

Example:

```javascript
const numbers = [1, 2, 3, 4, 5];

// map
const doubled = numbers.map(num => num * 2);
console.log(doubled);
// [2, 4, 6, 8, 10]

// filter
const even = numbers.filter(num => num % 2 === 0);
console.log(even);
// [2, 4]
```

---

## HR Interview Questions

### Tell me about yourself.

I am a Frontend Developer with around 4 years of experience in React.js development.

I have worked on enterprise applications involving dashboard development, API integration, reusable components, dynamic forms, authentication, and responsive UI development.

I also have exposure to Node.js and MongoDB for full-stack development.

Currently I am looking for an opportunity where I can contribute technically and grow further as a frontend/full-stack developer.

---

### Why should we hire you?

I have strong hands-on experience in React.js and real-time project development.

I can independently handle frontend modules, API integration, reusable component development, debugging, and production issues.

I also adapt quickly and work well in team environments.

---

### If you don't know the answer?

I haven’t worked deeply on that yet, but I understand the basics and I’m currently learning it.

---



